#  Data types

## Primitives and Built in:

Primitives
- `int` 32 bit integer
- `long` 64 bit integer
- `float` 32 bit float
- `double` 64 bit float
- `char` character

Built in:
- `String` string of `char`
- `(type) => type` closure